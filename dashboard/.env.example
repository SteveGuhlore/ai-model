# The FastAPI backend the dashboard proxies to. The browser only ever talks to
# this app's /api proxy — provider keys (FAL_KEY etc.) stay on the backend and
# never reach the client bundle.
API_PROXY_TARGET=http://localhost:8000
